{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/reichaves/due-diligence-transnacional/schemas/target.schema.json",
  "title": "Target",
  "description": "Input de uma investigação de due diligence transnacional. Preencher antes de iniciar o pipeline.",
  "type": "object",
  "required": ["nome_completo", "razao_investigativa"],
  "unevaluatedProperties": false,
  "properties": {
    "nome_completo": {
      "type": "string",
      "minLength": 3,
      "description": "Nome completo do alvo como consta em documentos oficiais do país de origem. Para alvos brasileiros: conforme CPF/RG. Para outros países: transliteração oficial usada em documentos."
    },
    "ocupacao": {
      "type": "string",
      "description": "Ocupação ou cargo principal do alvo (ex: 'CEO da Ferreira Global Ltda', 'Empresário do setor de petróleo')."
    },
    "empresa_principal": {
      "type": "string",
      "description": "Nome da empresa ou organização com a qual o alvo é mais associado."
    },
    "cidade_residencia": {
      "type": "string",
      "description": "Cidade de residência no país de origem, se conhecida."
    },
    "uf_residencia_us": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "UF americana (dois caracteres maiúsculos) se o alvo for residente nos EUA. Ex: 'FL', 'TX', 'NY'."
    },
    "parentes": {
      "type": "array",
      "description": "Parentes diretos relevantes para a investigação. Cada um receberá variações de nome próprias.",
      "items": {
        "type": "object",
        "required": ["nome", "relacao"],
        "unevaluatedProperties": false,
        "properties": {
          "nome": {
            "type": "string",
            "minLength": 2
          },
          "relacao": {
            "type": "string",
            "enum": ["conjuge", "ex-conjuge", "pai", "mae", "filho", "filha", "irmao", "irma", "outro"],
            "description": "Relação de parentesco com o alvo."
          }
        }
      }
    },
    "apelidos": {
      "type": "array",
      "description": "Apelidos, nomes de guerra, pseudônimos ou variações conhecidas do nome.",
      "items": {
        "type": "string",
        "minLength": 2
      }
    },
    "razao_investigativa": {
      "type": "string",
      "minLength": 30,
      "description": "Descrição factual de por que esta pessoa está sendo investigada. Vai para o log e o dossiê. Mínimo 30 caracteres. Ex: 'Gestor de refinaria no Texas investigado por superfaturamento em contrato com a Petrobras'."
    },
    "origin_country": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "default": "BR",
      "description": "País de origem do alvo (ISO 3166-1 alpha-2). Aceita qualquer país. Ex: 'BR', 'US', 'PE', 'CO', 'NG', 'CN', 'DE'. Default 'BR'. Afeta quais outlets de imprensa são incluídos nas buscas pela skill search-news-archive."
    },
    "investigation_language": {
      "type": "string",
      "enum": ["pt-BR", "en-US"],
      "default": "pt-BR",
      "description": "Idioma do dossiê e das comunicações do pipeline. 'pt-BR' para português brasileiro, 'en-US' para inglês americano."
    },
    "bases_prioritarias": {
      "type": "array",
      "description": "Bases a consultar. Omitir para usar todas (padrão).",
      "items": {
        "type": "string",
        "enum": ["fec", "lda-fara", "state-corps", "opencorporates", "news"]
      },
      "default": ["fec", "lda-fara", "state-corps", "opencorporates", "news"],
      "uniqueItems": true
    }
  }
}
